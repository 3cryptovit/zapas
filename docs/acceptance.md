# Проверка руками

Сценарии, которые нужно пройти глазами перед тем, как показывать проект.
Автотесты проверяют коды ответов, здесь проверяется то, что видит человек.

Каждый пункт: что сделать → что должно получиться. Если получилось
другое — это находка.

Быстрый путь: разделы 1, 2, 6 и 9 закрывают демо за десять минут.

---

## Где проверять

**Демо на проде** — https://vitalness.ru/zapas/, кнопка «Открыть
демо». Отдельная организация с полугодом истории, живёт сутки,
регистрация не нужна. Подходит для всего, кроме Telegram: в демо
внешние каналы отключены намеренно.

**Локально** — `make dev`, затем `make seed`, вход
`owner@zapas.local` / `локальный-пароль-42`. Нужно для проверки
Telegram и для работы с пустой организацией.

Ограничение: пять демо в час с одного адреса. Повторное нажатие кнопки
возвращает в уже открытое демо, а не создаёт новое.

---

## 1. Вход и первый экран

| Шаг | Ожидается |
|---|---|
| Открыть https://vitalness.ru/ | Портфолио, в списке проектов — Zapas |
| Нажать «Zapas» | Лендинг на `/zapas/`, кнопка «Открыть демо» |
| Открыть `vitalness.ru/zapas` без слеша и `www.vitalness.ru/zapas/` | Оба раза — лендинг на `https://vitalness.ru/zapas/` |
| Нажать кнопку | Через 1–2 секунды — дашборд, логин не спрашивают |
| Нажать «+1 день» | Дата в полосе песочницы сдвинулась. Это первый изменяющий запрос: если он отвергнут как «со стороннего адреса», сломана проверка Origin под путём |
| Посмотреть на шапку | Полоса песочницы: виртуальная дата, «+1 день», «+7 дней», автопилот, сброс |
| Открыть `/zapas/app/orders` напрямую в адресной строке | Экран заказов, не редирект на вход |
| Нажать «Выйти», затем открыть `/zapas/app/` | Экран входа |

Проверить в консоли браузера (F12 → Console): ошибок быть не должно.
Вкладка Network: все запросы 200 или 201, ни одного 403 и 404.

---

## 2. Дашборд

| Шаг | Ожидается |
|---|---|
| Посмотреть счётчики сверху | Пять чисел: нет в наличии, критично, заказать сегодня, в порядке, нет прогноза. Сумма равна общему числу позиций |
| Посмотреть порядок строк | Тревожные сверху: сплошная заливка, затем штриховка, внизу тонкий контур |
| Найти блоки «Заказать у поставщика» | Позиции сгруппированы по поставщикам, у каждого — время отсечки |
| Посмотреть на колонку количества | Два числа: в базовых единицах и в единицах заказа («2 кор.»), количество кратно упаковке |
| Найти позицию со статусом «нет прогноза» | Пунктирный образец и подпись: система честно говорит, что данных мало |

Ключевое: **ни одно количество не должно быть дробным там, где
поставщик возит коробками.** 1,3 коробки молока заказать нельзя.

---

## 3. Карточка позиции

Открыть любую позицию из дашборда.

| Шаг | Ожидается |
|---|---|
| График спроса | Тёмные столбики факта, пунктирная линия прогноза, светлая полоса коридора ±σ |
| Дни с нулевым остатком | Светлые столбики, подписаны как исключённые из обучения |
| График проекции остатка | Линия идёт вниз, пересекает ноль в дату «хватит до» |
| Блок «почему такой статус» | Все числа расчёта: остаток, в пути, позиция запаса, потребность, страховой запас, целевой уровень, недобор |
| Точность прогноза | Процент и модель (M0, M1 или M2) |

Проверка на согласованность: **целевой уровень минус позиция запаса,
округлённое вверх до кратности, должно совпасть с рекомендованным
количеством.** Если не сходится — это находка.

---

## 4. Движения

С карточки позиции или с дашборда.

| Шаг | Ожидается |
|---|---|
| Записать приход 10 | Остаток вырос на 10, статус пересчитался сразу, без обновления страницы |
| Записать расход 3 | Остаток уменьшился на 3 |
| Списать 1 без причины | Форма не даёт отправить, требует причину |
| Списать 1 с причиной «испортилось» | Записано, в ленте видна причина |
| Списать больше, чем есть | Отказ с сообщением, в котором указан реальный остаток |
| Записать движение завтрашним числом | Отказ: события из будущего не принимаются |
| Записать движение десятидневной давности | Отказ: дальше семи дней назад нельзя |

**Сторнирование.** Найти запись в ленте, нажать «Сторно».

| Шаг | Ожидается |
|---|---|
| Сторнировать приход | Появляется обратная запись, остаток вернулся |
| Сторнировать её же ещё раз | Отказ: повторное сторно запрещено |
| Посмотреть на исходную запись | Помечена как сторнированная, но не исчезла и не изменилась |

Журнал неизменяем: **записи не редактируются и не удаляются, только
гасятся обратной.**

---

## 5. Лента движений

| Шаг | Ожидается |
|---|---|
| Открыть «Движения» | Список с типом, количеством, автором и временем |
| Фильтр по позиции | Только движения этой позиции |
| Фильтр по типу «списание» | Только списания, у каждого видна причина |
| Пролистать вниз | Подгружается следующая страница |

---

## 6. Заказы

| Шаг | Ожидается |
|---|---|
| С дашборда нажать «Заказать» у поставщика | Черновик заказа со строками из рекомендации |
| Открыть заказ | Виден готовый текст для отправки поставщику |
| Проверить текст | Количества в единицах заказа, без хвостов вида «2.000 кор.» |
| Изменить количество в строке | Сохраняется, пока заказ черновик |
| Нажать «Отправить» | Статус «отправлен», на дашборде у этих позиций появилось «в пути» |
| Вернуться на дашборд | **Эти позиции больше не советуют заказать повторно** |
| Изменить отправленный заказ | Отказ: менять можно только черновик |
| Нажать «Принять» | Статус «принят», приход записан, «в пути» обнулилось |
| Принять с недовозом (указать меньше) | Приход на фактическое, расхождение зафиксировано, появилось уведомление |
| Нажать «Отправить» у принятого | Отказ |

Ключевое: **пока заказ в пути, система не советует заказать то же
самое второй раз.** Это и есть смысл поля «в пути».

---

## 7. Инвентаризация

| Шаг | Ожидается |
|---|---|
| Открыть «Инвентаризация» → «Начать пересчёт» | Список всех позиций с ожидаемым остатком |
| Ввести количество, нажать Enter | Курсор переходит к следующей позиции |
| Уйти со страницы и вернуться | Введённое сохранилось |
| Ввести расхождение и провести | Созданы корректирующие движения на разницу |
| Открыть ленту движений | Корректировки видны с типом «корректировка» и ссылкой на пересчёт |
| Провести пересчёт, если остаток изменился во время ввода | Предупреждение и запрос подтверждения |

---

## 8. Справочники и импорт

| Шаг | Ожидается |
|---|---|
| Настройки → создать категорию | Появилась в списке |
| Создать поставщика с днями доставки и отсечкой | Появился, дни отмечены |
| Создать позицию и привязать к поставщику | Появилась на дашборде со статусом «нет прогноза» |
| Задать условия закупки (единица, кратность) | Рекомендации по этой позиции округляются до кратности |
| Архивировать позицию | Пропала из списка, но её движения в ленте остались |

**Импорт CSV.**

| Шаг | Ожидается |
|---|---|
| Загрузить файл с ошибками в строках | Предпросмотр: что будет создано и список ошибок с номерами строк |
| Нажать «Импортировать» | Созданы только корректные строки |
| Загрузить тот же файл повторно | Дубликатов не появилось |
| Сохранить файл из Excel как обычный CSV (CP1251) и загрузить | Импортируется, кириллица целая |
| Загрузить не тот файл (картинку, архив) | Понятное сообщение, а не ошибка сервера |

Последние два пункта — прямое следствие находки: Excel на русской
Windows по умолчанию пишет CP1251, и раньше такой файл ронял импорт.

---

## 9. Песочница и время

Самое наглядное для показа.

| Шаг | Ожидается |
|---|---|
| Запомнить виртуальную дату и счётчики | — |
| Нажать «+1 день» | Дата сдвинулась, часть позиций израсходовалась, счётчики изменились |
| Нажать «+7 дней» без автопилота | **Тёмных строк заметно больше**: заказы никто не оформлял |
| Включить автопилот, нажать «+7 дней» | Заказы оформились и приехали сами, тёмных строк мало |
| Открыть «Уведомления» | Сводки за прошедшие дни, алерты по критичным позициям |
| Нажать «Начать заново» | Свежее демо с исходными данными |

Ключевое: **сравнение «с системой и без неё»** — это главный аргумент
всего проекта, и он виден за минуту.

---

## 10. Уведомления

| Шаг | Ожидается |
|---|---|
| Открыть «Уведомления» | Лента: сводки, критичные позиции, напоминания об отсечке, опоздания |
| Открыть уведомление | Ссылка ведёт на нужную позицию или заказ |
| Нажать «Прочитано» | Счётчик непрочитанных обнулился |
| Проверить формулировки | Нет несогласованных фраз вроде «Стаканы закончится завтра» |

---

## 11. Telegram

Только на настоящей организации, не в демо.

| Шаг | Ожидается |
|---|---|
| Настройки → «Привязать Telegram» | Ссылка вида `t.me/zapas_tg_bot?start=<код>` |
| Перейти по ссылке, нажать Start | Бот отвечает, что чат привязан |
| В настройках обновить страницу | Отмечено, что Telegram подключён |
| Перейти по той же ссылке ещё раз | Код уже использован, повторно не срабатывает |
| Отправить боту `/stop` | Чат отвязан |
| Попробовать привязать из демо | Отказ: в демо внешние каналы отключены |

---

## 12. Изоляция организаций

Главное требование безопасности. Нужны две организации: открыть демо в
обычном окне и второе — в режиме инкогнито.

| Шаг | Ожидается |
|---|---|
| Скопировать id позиции из первого демо | — |
| Во втором демо открыть `/zapas/app/items/<чужой id>` | «Не найдено», а не чужие данные |
| Сравнить списки позиций | Разные наборы, ничего общего |
| Выйти и открыть `/zapas/app/` | Экран входа |

Ответ на чужой ресурс — **404, а не 403**: по 403 можно узнать, что
такой объект существует.

---

## 13. Монохром: статусы без цвета

Интерфейс чёрно-белый, поэтому состояние несут заливка, подпись и вес
шрифта. Проверять это нужно отдельно: ошибка здесь не ломает работу, но
делает главный экран нечитаемым.

| Шаг | Ожидается |
|---|---|
| Открыть дашборд, где есть все пять статусов | Квадраты-образцы слева от подписей различаются: сплошной, плотная штриховка, редкая штриховка, пунктир, тонкий контур |
| Отойти от экрана на два шага | Тревожные строки всё ещё выделяются пятном, не вчитываясь в текст |
| Открыть список заказов | Черновик — пунктир, отправлен — штриховка, принят — сплошная заливка, отменён — зачёркнут |
| Открыть уведомления | Чем срочнее, тем толще линейка слева |
| Открыть карточку позиции | На графике различимы: столбики факта, светлые столбики исключённых дней, пунктир прогноза, серая полоса коридора |
| Напечатать дашборд в PDF (Ctrl+P) | Штриховка сохранилась, статусы читаются на бумаге |
| Включить в системе оттенки серого | Ничего не изменилось — цвета и так нет |

---

## 14. Мелочи, которые обычно ломаются

| Шаг | Ожидается |
|---|---|
| Открыть на телефоне | Таблицы читаемы, формы не уезжают за край |
| Обновить страницу на любом экране | Возвращается тот же экран, а не дашборд |
| Нажать «Назад» после записи движения | Движение не записывается второй раз |
| Записать движение при выключенном интернете и включить обратно | Одно движение, не два |
| Оставить вкладку на час и вернуться | Либо работает, либо просит войти заново — без белого экрана |

---

## Что уже проверяется автоматически

Эти сценарии закрыты тестами и в ручной проверке не нуждаются:

- изоляция организаций на уровне базы — политики RLS, отдельная роль
  без права их обходить;
- конкурентность — 50 одновременных списаний одной позиции;
- идемпотентность — повтор запроса с тем же ключом не создаёт второе
  движение;
- согласованность — остаток равен сумме движений по всем позициям;
- точность прогноза — WAPE не хуже 20% на данных с фиксированным seed;
- скорость — дашборд и запись движения на базе с миллионом движений
  (см. [performance/](performance/README.md));
- кодировки CSV — UTF-8, UTF-8 с BOM, CP1251, двоичный файл, UTF-16;
- промотка времени с автопилотом на две недели подряд.

```bash
make test        # 13 пакетов под детектором гонок
make api-lint    # контракт API
```
